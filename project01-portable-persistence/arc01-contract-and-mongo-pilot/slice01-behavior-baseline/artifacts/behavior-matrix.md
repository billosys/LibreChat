# Behavior and coverage matrix

Baseline commit: `ba44443fdb232bbe6d4977e2619774b5a72586ac`. **Observed** below means inspected current source; **executed** means a named existing assertion passed in this contributor's retained run. Neither label means independent acceptance. **Proposed** items are next-slice design inputs.

Test keys: **M** = `packages/data-schemas/src/methods/message.spec.ts`; **C** = `methods/conversation.spec.ts` in the same workspace; **S** = `packages/api/src/conversations/save.spec.ts`; **T** = tenant conformance; **G** = data-schemas `convoStructure.spec.ts`; **B** = `api/app/clients/specs/BaseClient.test.js`; **R** = `api/server/routes/__tests__/messages-get.spec.js` and `messages-get-real-validation.spec.js`; **I** = the three selected import suites. Exact full names/statuses are in [assertions.tsv](assertions.tsv); raw outcomes and denominators are in [test-baseline.md](test-baseline.md). Source locators refer to the [dependency map](dependency-map.md).

| ID | Trigger and current behavior | Source evidence | Existing coverage / limitation | Proposed contract consequence |
|---|---|---|---|---|
| B01 | Message create/update uses logical message ID plus authoritative user, tenant added by plugin. Return contains Mongo `_id` as well as logical identity. | `message.ts:848–1056`; message schema unique index | Executed M “should save a message for an authenticated user”; concurrent first-save test asserts one row. Complete two-owner/two-tenant collision matrix is still needed for the new adapter. | Logical IDs and owner/scope are public; storage references stay adapter-private. Do not assume global logical-ID uniqueness. |
| B02 | Missing user throws; invalid/missing conversation UUID-shaped string returns undefined, not a successful write. Other fields can pass through Mongoose casting/strictness. | `message.ts:867–881` | Executed M unauthenticated, invalid ID, and no-sensitive-param-log cases. No exhaustive schema-invalid field corpus. | Explicit rejected/no-write outcome; distinguish validation from storage failure. Preserve old boundary behavior through compatibility translation. |
| B03 | `contextMeta` omitted preserves it; explicit null removes the stored field in the same update as terminal text, including provenance-merge path. Empty object is not the same command as null. | `message.ts:939–947, 277–374` | Executed M two atomic-unset cases (240–310), including stored-state checks. No exhaustive absent/undefined/null/empty matrix in the inspected tests. | Typed keep/set/clear semantics; no generic JSON merge that collapses states. Public DTO excludes private contextMeta; server DTO retains it. |
| B04 | NaN tokenCount is normalized to zero. Nested context usage preserves unknown extension fields and known zero versus missing. | `message.ts:947–953`; schema metadata | Executed M “round-trips context usage metadata without narrowing tool fields or unknown data.” Dedicated token-NaN/finite/infinite matrix not identified in selected assertions. | Explicit normalization/extension policy; unknown measurement is not zero. |
| B05 | Provenance independent of role; unknown legacy value remains unknown; model output stamp only when appropriate; normalized paths union atomically with bounded CAS. | `message.ts:17–108,196–374,954–980` | Executed M provenance validation, concurrent first saves, stale-snapshot retry via updateMessage, legacy unknown preservation, dollar-prefix literal and update-array rejection cases. Eight-attempt exhaustion for ordinary save is source-observed, not directly proved by this baseline. | Preserve provenance states and merge semantics. Do not turn submitted/imported assistant content into trusted native output. |
| B06 | Duplicate-key recovery may return the existing row; reread failure or absent row returns undefined. Returning a row does not prove a failed update applied. | `message.ts:1030–1053` | Executed concurrent first-save case covers convergence through the provenance path. Exact outer duplicate-message-text match and recovery-reread failures remain gaps. | Separate already-present/recovered result from confirmed application where needed; decide retry meaning before freezing API. |
| B07 | `saveConvo` default upserts; `noUpsert` missing row returns null. It protects insert-only initial agent and code choice, actor-owned fields, and project membership. | `conversation.ts:2148–2557` | Executed C noUpsert create/update/default tests; immutable attribution, actor checkpoint rejection, code decision and project-stat tests. | Explicit insert policy and protected server metadata; child writers must not resurrect deleted parents/threads. |
| B08 | Absent append option rebuilds embedded IDs from Message query; [] avoids query/append; populated uses deduplicating `$addToSet`. Caller `messages` ignored when append is present. | `conversation.ts:2202–2207,2330–2333` | Executed C append tests (1353–1441); S seed checks [] creates empty list without Message.find. | Express logical link intent without exporting ObjectId arrays. Preserve omission versus known-empty. |
| B09 | Missing endpoint options may cause selected old endpoint fields to be unset. Protected/excluded keys survive; agent model has special handling. | `save.ts:93–115` | Executed B existing-conversation/unset tests assert generated unset intent. C “should handle unsetFields metadata” never first stores the field being cleared (501–513), so it is **weak evidence** of actual deletion. | Add a discriminating seeded-known-field clear test and protected-field control; do not treat the test title alone as coverage. |
| B10 | Request property absent means unread; present-null means known missing; present-row means loaded. Successful writes refresh cache. Initialized flag skips lookup and unset reconstruction; it is not synonymous with save success. | `save.ts:54–82,124–168` | Executed B “saveMessageToDatabase reuses conversation resolved on the request,” existing-row read-count tests; S seeded-row reuse. Explicit cached-null × failure × second-write cases remain necessary. | Carry lookup state explicitly or preserve property presence; do not coalesce null to unread and add serial queries. |
| B11 | Temporary/general retention comes from trusted context, not message payload; explicit valid deadline reused. Omitted chat type preserves stored deadline/flag; some legacy state is backfilled after main write. | `message.ts:888–935,992–1025`; conversation counterpart; retention helpers | Executed M/C “isTemporary …” cases including omitted true/false deadline preservation and all-mode legacy cases; R spoofed payload uses guard-loaded retention. TTL expiry timing and backfill-write failure not exercised. | Resolved retention intent and clock policy belong in contract design; preserve existing deadlines across updates and backend replacement. |
| B12 | Seed creates conversation before deferred first message, skips existing row, uses [] append and creation-time-on-insert, never creates child row through noUpsert. Seed exceptions settle. | `save.ts:185–212` | Executed S all seven seed cases. No DB-wide transaction asserted. | Keep admission/seed lifecycle separate from an atomic “save whole turn” promise. |
| B13 | Pending seed delays deferred write; completed seed starts callback synchronously. Stop starts pending parent persistence; policy rejection can cancel it. Terminal-ownership denial prevents final response save. | `BaseClient.js:912–1003,1238–1253`; `save.ts:219–229` | Executed S two ordering cases and B deferred/Stop/admission/terminal-ownership cases with controlled promises. Provider and multiprocess failure not tested. | New async service boundary must preserve ordering and ownership; establish durability timing explicitly. |
| B14 | Client disposed before call yields {}; disposal during await is safe because options were snapshotted. `skipSaveConvo` returns message only. | `BaseClient.js:1332–1395` | Executed B disposed-before and mid-await disposal cases; seed skip case. Full real-storage disposed/partial-turn integration remains open. | Capture required request context before suspension; represent skipped writes without pretending persisted success. |
| B15 | Message saves before conversation. Conversation failures are caught into error object; project-stat/backfill failure may follow committed row. User-message caller may swallow thrown write errors into {}. | `BaseClient.js:912–918,1352–1381`; `conversation.ts:2541–2557` | Executed C database-disconnect error test returns `{message: 'Error saving conversation'}`. No full injected-failure-after-message / after-conversation / after-project-update integration in selected suites. | Explicit partial outcome and safe retry contract. Preserve legacy externally observed behavior during pilot; do not claim atomicity or silently upgrade it. |
| B16 | Tenant scope is ambient today: scoped injects, system bypasses, unscoped permitted unless strict mode. Cross-tenant update mutation rejected; bulk wrapper strips instead. | tenant policy/plugin/bulk wrapper | Executed 13 T cases; M/C tenant stripping and scoped provenance tests. New transport-boundary spoof/replay cases not yet present. | Pass validated actor+scope context explicitly; keep system authority distinct from missing context. |
| B17 | Server history queries rows, walks logical parents, guards cycles, can stop at checkpoint summaries; root sentinel avoids DB. Embedded conversation-ID list is not required for reload. | `BaseClient.js:1256–1324,1437–1508`; `message.ts:2397–2421` | Executed B three loadHistory cases and user-scoped reads; G five DB/tree cases, including scrambled dates and >16 messages. | Storage query and graph/context assembly are separate responsibilities. Preserve parent graph independent of timestamp sort. |
| B18 | Public reload gates owner-scoped read on validation, hides expired/child records, strips server-only fields. Active owner job can permit read before conversation row exists. | routes/messages 93–217,492–519; ownership method; validation wiring | Executed R ownership, concurrent reads, failed-validation nondisclosure, missing/child equivalence and active-job exception. Executed M projection and C expiry ownership cases. Route storage is mocked, so cross-layer real-DB proof still needed. | Separate server/public projections and authorization probe; retain concurrent reads without returning unvalidated data. |
| B19 | Ordinary getMessages sorts createdAt only. Cursor read sorts one chosen field and uses strict greater/less cursor filter; no identity tie-breaker. | `message.ts:2397–2421,3583–3619` | R tests routing/parameters, not real cursor population completeness. C tie-break tests cover **conversation pagination**, not this message cursor method. | **Hypothesis:** equal-key messages at a page edge can be skipped. Add equal-time boundary oracle; any correction is separately reviewed scope, not silently part of the adapter. |
| B20 | Optional search indexing runs detached after storage hook; async indexing failure is separate. Schema defaults, private projections, casting, TTL/index uniqueness affect results. | model/schema/plugin sources | Baseline tests core models, not external Meili availability or real TTL elapsed cleanup. | Adapter preserves current storage/plugin lifecycle first; expose only guarantees we can support. Search/retention services need later explicit backend capability contracts. |
| B21 | Claude import replaces identities, linearizes retained messages, skips absent rendered text/thinking, concatenates thinking, adjusts timestamps and assigns configured model label. Bulk import does not populate embedded message list. | importer/builder and [reconciliation](../../../corpus-reconciliation.md) | I includes Claude mapping/timestamp assertions; original corpus reconciliation covers all 820 conversations and 9,838 source messages. Reconciliation is prior self-checked evidence, not a fresh run here. | Preserve raw source and derivation alongside current operational representation. Migration cannot reconstruct data discarded by importer. |
| B22 | Import validates BSON size before writes; writes conversations then messages; failures compensate, tag-count failure is logged. Cleanup failing at messages stops before conversation cleanup; original failure rethrows. | `packages/api/src/conversations/import.ts:53–122`; builder 176–245 | Executed I database roundtrip and oversized-record no-write cases. Detailed rollback/cleanup-failure helper tests exist separately but were not run in this slice. | Bulk ingestion needs separate batch/outcome/recovery capability; do not call ordinary upsert “import.” |

The relevant baseline is green with **named coverage gaps**, not complete conformance coverage. Gaps above are design work items, not failed tests or waived requirements. The next slice owns deciding the corresponding oracles and acceptance policy; implementation must supply those tests before claiming the new boundary preserves these behaviors.
