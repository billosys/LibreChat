# CC iteration04 — preserve verified guards and complete declaration applications

## Assignment, authority and output fence

Execute the **fourth correction** of Project01 / Arc01 / Slice02 as CC, returning to the existing CDC design conversation through the Operator. Predecessor: [cc-prompt-iteration03.md](cc-prompt-iteration03.md), delivered at `76d15b5d33668e3c50ddd3db7b98a8e6c12a814c`. Read the current [CDC review](cdc-verification.md#correction-iteration03-review--2026-09-22) and its [counterexamples](artifacts/cdc-review04/result.json). This is evidence repair, not application implementation or slice closure. R3 query observations remain reproduced. R1 has regressed; R2 has substantive repairs and remaining defects. S01–S07 remain open.

Paths: `PLAN=/Users/oubiwann/lab/billosys/LibreChat/.worktrees/planning` on `planning`; `SOURCE=/Users/oubiwann/lab/billosys/LibreChat/.worktrees/billo-guildhall` on `billo-guildhall`; `SLICE=$PLAN/project01-portable-persistence/arc01-contract-and-mongo-pilot/slice02-portable-contract-design`; `OUT=$SLICE/artifacts/cc-evidence05`. Pin source to `3e3c5410d3863118fdba694fb0cd51baeb7102f9`. Node is `/Users/oubiwann/.local/bin/node`, v22.22.3; compiler is `/Users/oubiwann/lab/billosys/LibreChat/node_modules/typescript/lib/typescript.js`, 5.9.3, SHA-256 `3ae902c92cc44dace175c0e69e13a4b0899f6983c6121d76b9ab8dd5795e7675`.

Create only seven new files under OUT: `intake.md`, `report.md`, `nested-fields.json`, `replay-result.json`, `execution.log`, `replay.py`, `SHA256SUMS`. Preserve all prior evidence/reviews/prompts and plans byte-for-byte. No source/dependency edits, database/private-export access, builds/application suites, installs, services, pushes, PRs or framework edits. Keep helper code inside replay.py; keep structured declarations, graph and checks inside the permitted JSON/log. Do not copy private exports into evidence. Preserve unrelated index/worktree content. CDC owns codec/DTO/patch decisions, composition/injection, D07 and sizing. Missing inputs or an unsupported source construct block the affected claim; record the exact gap rather than invent a result.

The binding outcome is unchanged: 117 top-level fields, ten selected roots and twelve nested public exclusions, with source-backed nested declarations and application semantics. There is no required declaration/member count. Do not remove findings or reset iteration accounting. This assignment is the fourth correction, following three completed corrections; the existing five-correction cap remains in force.

## Required reading and intake

Read in the following order, using complete bounded output and recovering truncation. Record source state, actual loaded extents, data-query commands/results and a concise source-cited contract readback in intake.md. Receipts do not prove semantic correctness. No additional permission exchange is needed after coherent preflight.

| Class | Exact input and scope | Purpose and required application |
|---|---|---|
| Required-full | This prompt; PLAN AGENTS.md, BILLO.md, README.md; SOURCE AGENTS.md, BILLO.md, CLAUDE.md; slice-plan.md and ledger.md | Verify current assignment, branches, seven-file scope and all open acceptance gates. Source quality rules do not authorize source edits. |
| Required-section | Project plan: Goal and boundaries, Definition of done, Workflow and authority including nested transition/Expedited sections. Arc plan: Scope, Slice roadmap, Workflow and source boundaries. | Preserve ownership and production boundaries. |
| Required-section | cdc-verification.md: Correction iteration03 review in full. Required-full: artifacts/cdc-review04/review.py. Required-data: its result.json, all controls, classifications, source witnesses, manifest results and replay/query outputs; inspect corresponding execution.log records completely. | Reproduce the actual acceptance failures, not just report counts. |
| Required-full | artifacts/cc-evidence03/replay.py; artifacts/cc-evidence04/replay.py, intake.md and report.md | Restore evidence03 preflight/classification/preservation guarantees while retaining evidence04's valid extracted declarations. Both packets are historical inputs; neither is a complete correct implementation to copy blindly. |
| Required-data | evidence04 nested-fields.json: all 117 coverage rows, ten roots, member rows, 46 declaration records including text/properties, projection and unresolved records; all replay-result.json fields. pass05 field-matrix.json: every records[].fields[] entry and each record's providerOnly/interfaceOnly/implicit; complete baseline result-01.json. | Partition by root/declaration/record with complete coverage. Compare exact identities and values, retaining denominators and errors. No sampling in place of full coverage. |
| Required-full | artifacts/design-pass05/read-contract.md, protocol.md, experiment.cjs, field-matrix.cjs, validate.py; artifacts/design-pass04/handle.cjs | Read-view meanings, sealed replay and the limits of historical validation. |
| Required-section | `/Users/oubiwann/.codex/skills/engineering-methods/guides/07-implementation-prompt-authoring.md`: Investigation and semantic evidence assignments, Design the tests and their oracles, Required reading and CC intake. Required-full: `/Users/oubiwann/.codex/skills/work-verification/guides/05-independent-verification.md`; `/Users/oubiwann/.codex/skills/scientific-methods/guides/06-evidence-capture.md` and `08-analysis-and-threats-to-validity.md`. | Separate source syntax, interpretation, observation and acceptance; test the actual verifier and keep raw failed attempts. |
| Required-section | `/Users/oubiwann/.codex/skills/collaboration-framework/knowledge/engineering-methods/guides/01-engineering-methodology.md`: Roles and Shared Invariants, Two-Contributor Workflow, Workflow Selection and Transitions. | CDC/CC/Operator separation; no CRC or self-acceptance. |
| Required source | Complete declarations underlying the ten roots and local dependencies, including the table below; complete CLIENT_MESSAGE_SELECT and getConvoOwnership. Include message/conversation storage schema initializers, preset/fading definitions and direct IMessage/IConversation definitions used by selected fields. | Distinguish storage from provider shape; establish actual paths, local and effective requiredness, view exclusions and local reference closure. Record exact file/symbol/span/digest. |
| Conditional | Full enclosing function plus relevant helpers before any producer/consumer claim beyond the named read methods; additional local declaration reached by a required reference before asserting closure. | Keep type-only claims separate from observed uses. At external imports/open records, record the exact boundary without surveying external SDKs. |

Older prompts/reviews not named above are reference-only history; their binding scope and guard requirements are carried explicitly here. A discovered conflicting authority is a blocker to the affected step, not a choice of convenient text. Revalidate/reload required contracts after context loss. Do not record retrospective timestamps or claim that writing a large query result to a log loaded its contents into context.

## 1. Restore R1 before extending R2

**Binding:** start from the verified behavior of evidence03's `collect_environment`, `verify_environment`, `expected_label`, `packet_inputs`, `verify_packet` and self-test controls. Port those obligations into the new packet. Do not replace them with evidence04's optional `observed` branch or its capture-only preflight. Helpers may be reorganized, but retain a correspondence table and prove each previous guard still executes.

Verification order is: read-only packet inventory/manifest and pinned input checks; fresh source branch/head/status, Node version and compiler identity; submitted source fingerprints; submitted-vs-baseline result comparison; fresh declaration/application validation; fresh sealed harness; fresh-vs-submitted and fresh-vs-baseline; final packet hash equality. Every dependent extractor/harness invocation must follow successful environment/input preflight. Temporary output only. Verify/self-test must never seal or rewrite the supplied packet. Capture refuses an already sealed output.

Use a real production-path injection seam for controls: `verify_packet(packet, environment_reader=collect_environment, runner=run_harness)` is a recommended interface, not tested drop-in code. The ordinary CLI must collect real observations by default. The injected reader supplies a complete observation with exactly one bad value. Assert runner calls **zero** for each invalid branch/head/status/Node/compiler/input-fingerprint case and **one** for a valid control. A reader/guard that is never called fails even if a separate helper rejects a manually supplied dictionary. Mutate a disposable packet during the injected runner and assert final verification rejects. Never mutate the original packet.

Restore exact field classification from the matrix: selected `(record, field)` in the ten-root set first; `('conversation','messages')` is the private physical relationship; then `defaultRead == 'hidden'`; otherwise other. Normalize `messageSchema` to `message` and `convoSchema` to `conversation` once. Expected current counts are 10 selected, 14 hidden, 1 relationship, 92 other. Counts supplement exact-key/label comparison. Reject duplicate roots/coverage/member/declaration identities before creating dictionaries; dictionary overwrite is not validation.

Retain R3's actual jq projection `.records[] | {name, fields: [.fields[].name], providerOnly, interfaceOnly, implicit}`. Compare complete results to the pinned matrix, not only counts: 44/73 fields, 10/13 provider-only, 4/3 interface-only, four implicit keys each. Wrong names/levels remain negative controls. Preserve submitted result/provenance and all previously valid controls. Inspect guard changes as a diff against evidence03; no scope or runtime expansion.

## 2. Complete the source-to-application mapping

**Binding:** preserve declaration syntax separately from application semantics. The 46 declarations in evidence04 are useful input, but fresh equality with the same generator cannot establish that the generator interpreted them correctly. It currently drops some required local definitions and application branches and emits expressions such as `[Tools.web_search]` as data paths.

**Recommended representation:** retain exact declaration records, add explicit reference/structural edges and use sites, and derive member applications from those. Reuse a local declaration table at several prefixes rather than copying whole subtrees. Each reference must resolve uniquely by file/namespace/symbol, terminate at a named external/open boundary, or be marked blocked. A full TypeScript typechecker or general-purpose schema compiler is not requested. Preserve only constructs reached by these ten roots; do not evaluate application/Zod modules. Follow cycles with recorded reference edges instead of unbounded expansion.

The bounded syntax walker must represent type literals, interfaces, named references, unions/intersections, both `T[]` and `Array<T>`, records/index signatures, Pick and shallow Partial, property optionality, computed keys whose local enum values are known, and Zod object/array/record/optional/nullable/default/reference/extend expressions encountered here. Read AST structure instead of searching an entire initializer string for `.optional()` or `null`: a nested optional child does not make its containing member optional. Preserve an unsupported construct verbatim with a named blocker; do not silently treat it as closed, optional, external or empty. Resolve symbol ambiguity explicitly rather than returning the first textual match.

A member application needs the literal path or structured path segments, declaration/branch identity, local type and optionality, applicable ancestor conditions, and effective optionality after its wrapper. Keep null, undefined and absence separate. Validate every such semantic field and all source references/fingerprints, not just path/type and one file_id special case.

### Worked application examples

These are source-grounded expected interpretations, not executed generator code. Their exact source syntax is retained in the CDC review witnesses; recheck it at the pinned source.

| Source/use site | Required interpretation | Plausible wrong result the check must reject |
|---|---|---|
| `TMessage.files?: Partial<TFile>[]`; TFile.file_id and filename are required locally | `files[].file_id` and `.filename`: local required, effective optional at this use site; files is an optional array. The shallow wrapper does not make RunFileProvenance.runId optional within a supplied metadata.runFile object. | Changing filename's effective requirement to required while leaving file_id correct; replacing the wrapper with TFile[]; applying Partial recursively to runId. |
| `SummaryContentPart.content?: Array<{ type: ContentTypes.TEXT; text: string }>` | `content[].content[]` has local element members `.type` and `.text`, both required inside a supplied element. The SUMMARY discriminator belongs to the outer branch. | Parent-only Array text treated as a complete element map; a fabricated `.summary` wrapper. |
| TAttachment's three alternatives (`schemas.ts:1054–1061`) | Branch A: TFile & metadata. B: Pick filename/filepath/conversationId, required numeric expiresAt & metadata. C: shallow Partial of Pick filename/filepath, unchanged Pick conversationId & metadata. Shared metadata is intersected into all three branches. | Omitting C; globally requiring filename/filepath; detaching numeric expiresAt from B; placing branch labels in data paths. |
| `TAttachmentMetadata[Tools.web_search]?: SearchResultData` and file_search | Resolve Tools values from their local declaration; actual keys are web_search/file_search. Link both use sites to SearchResultData. Follow organic[] → ProcessedOrganic → OrganicResult & ProcessedSource, and topStories[] analogously. References/Highlight retain their array/record edges. | Literal `[Tools.web_search]` path, placing ProcessedSource.content at web_search root, or calling local SearchResultData an open/external boundary. |
| `CodeEnvRef = CodeEnvRefBase & {kind:'skill';version:number} | ...agent... | ...user...` and CodeEnvRefMap | Base members shared across branches; skill requires version, agent/user have only their declared branch additions. Map is a partial record of CodeEnvRef values. Named graph edges may express this without flattening every map key. | Keeping only CodeEnvRefBase and claiming the union application is complete; making version common to all branches. |
| `CLIENT_MESSAGE_SELECT` contains `-contextMeta` and `-metadata.thoughtSignatures` | Public contextMeta is excluded at the root; public metadata is retained with its nested exclusion. Full getConvoOwnership selects subagentThread. | Generic “included” for all message roots, whole-root metadata exclusion, or reduced lineage described as the current method's output. |

Before expanding the full map, run these representative positive cases and well-shaped negative mutations through the actual submitted-inventory validator. Correct the representation and checks until they discriminate the cases, then complete every root. This is an internal execution step, not a new handoff or permission gate. Retain the observed results, including failures.

### Local closure and root reconciliation

Keep all ten roots from iteration03. Reconcile every evidence04 member into retained/corrected/removed plus any source-derived additions; give reasons and locators for removals. Do not inflate counts. Reusable graph references can replace repeated flattened rows, but must still make each required application resolvable.

- **content:** complete applicable TMessageContentParts branches, including shared PartMetadata/ContentMetadata, local CodeToolCall/FunctionToolCall/RetrievalToolCall/FileSearchToolCall, Text/TextData, ImageFile, Summary and Steer definitions. Agents references follow their local alias before an actual external boundary. Preserve required Date leaves under optional backgroundTask/resultClaim, approval members and public exclusions.
- **files:** TFile, RunFileProvenance, FileContext/FileSources enum domains, CodeEnvRefBase/CodeEnvRef/CodeEnvRefMap and their local scalar dependencies. A declaration existing elsewhere in the inventory needs an explicit application edge. Retain storage/provider distinctions and shallow Partial semantics.
- **attachments:** all three branches above and TAttachmentMetadata. Follow local WorkspaceChange, MemoryArtifact, UIResource (known members plus its open index signature) and SearchResultData's local member types. For search references/highlights, keep actual element/record positions and distinct web/file projection treatment. No missing named local dependency may be relabeled open merely to finish the graph.
- **metadata:** open message record; do not transfer TFile metadata fields. Bound historical-value uncertainty explicitly.
- **feedback:** preserve the difference between Zod input and TFeedback/TFeedbackTag representations, including referenced local rating/tag declarations and their own presence rules. Do not infer optionality merely from a textual `undefined` union.
- **contextMeta:** direct/storage/provider definitions and fading schema; optional root and local leaves; fadingTiers array/extension member; excluded from public message output.
- **userSubmittedMessageFieldPaths:** exact strict path/field members and local HITL_MESSAGE_FILTER_FIELDS enum source; no source/operation keys.
- **examples:** input/output with required content strings; provider shape is not a restriction on historical Mixed storage.
- **codeWorkspaces:** optional root, entries require environmentId/workspaceId; distinguish storage and provider validation.
- **subagentThread:** optional root, parentAgentId optional, other seven local lineage members required; actual full ownership-probe selection.

Record declaration defaults/nullability separately from stored observations. Current source does not determine historical BSON/Mixed populations or select a transport codec. Root schema/direct-type claims need the actual storage/direct/provider reference, not a provider declaration mislabeled as the storage schema.

## 3. Validate projections as a composed set

Use structured paths/removal obligations, not prose strings containing `except`. Retain human explanations in a separate field. Canonicalize array annotations for exclusion comparison, but retain array structure in evidence. Read all CLIENT_MESSAGE_SELECT root and nested exclusions for applicability; the nested denominator remains twelve.

For every survivor S: reject if equal to/below any excluded E. Compute `required_removals(S) = {E | E is a strict descendant of S}` from the entire exclusion set. If nonempty, S must have exactly those removal obligations; if empty, it must have none. Apply this to **every survivor**, including metadata and organic/topStories containers. Reject missing container records, duplicate/conflicting records, omissions, extras and bypass syntax. Checking `submitted_removals <= actual_removals` is insufficient; equality is required. A parent survivor must not erase a child's exclusion. Web-specific exclusions must not be copied to file_search.

Positive: organic[] survives with both sitelinks and highlights removed; topStories[] survives with highlights removed; metadata survives with thoughtSignatures removed. Negative: no obligations; only sitelinks; resultClaim as a completionWakeup survivor; exact/descendant excluded paths; a falsely fully preserved container; a freeform `except` suffix that bypasses checking. Validate the actual submitted structure and report clear evidence errors.

## 4. Controls, sealing and return

Retain the evidence03 guard/control behavior and evidence04 valid repairs. Add every accepted CDC counterexample in review04 as a regression. Clone a valid row and alter one semantic value when testing semantics. Two old controls failed at missing `sourceRefs`; replace them with structurally valid nonexistent-path mutations. Do not treat arbitrary KeyError/TypeError/AssertionError as success. Expected evidence failures need a specific reason; unexpected exceptions fail the self-test. The wrong-file Partial control must change **effective use-site requiredness**, not falsely make the local TFile declaration optional.

Check the complete submitted declaration graph against fresh syntax/closed member sets and validate application edges/wrappers separately. Keep source-backed expected examples independent from build_inventory. A regenerated map equal to itself is a consistency check, not the semantic oracle. Report exact positive/negative counts without treating a target count as acceptance. Raw argv/cwd/status/stdout/stderr and failed attempts belong in execution.log; seal only after capture/report/intake are final.

Run the actual sealed CLI `--verify --packet "$OUT"` and `--self-test --packet "$OUT"`; retain their output outside the sealed packet and report it through the Operator. Require unchanged hashes and clear invalid-input errors. Verify all prior Project01 manifests/source/issued prompts remain unchanged, seven-file commit scope, Markdown links and `git diff --check`. No Mongo/source-build claim: Node 22 storage-double evidence still does not satisfy D07/Node24 or integrated conformance.

In report.md, map each R1/R2 subfinding to exact repair and reproduced evidence, account for all ten roots and prior rows, and retain R3 query results and all open S01–S07/CDC policy work. If a required declaration application cannot be supported, return its exact blocker and source evidence, not proposed-done completion of that criterion. Do not open Slice03 or silently split/re-scope the assignment.

Inspect the index; commit only the seven explicit OUT file paths using `git commit --only -F <message-file> -- <seven paths>` on planning. End with both trailers exactly once:

```text
Co-authored-by: Codex <noreply@openai.com>
Co-authored-by: Billo AI <ai-engineering@billo.systems>
```

Return commit, report/manifest paths, raw final verification output locations, failures/blockers and limits through the Operator. Proposed-done remains pending CDC review and Operator acceptance. At nonconvergence, preserve the actual correction count and surface sizing/method concerns; do not reset the budget.
