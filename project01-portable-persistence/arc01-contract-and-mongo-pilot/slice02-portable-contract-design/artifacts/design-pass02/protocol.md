# P02 — Logical-reference fast-path feasibility

Declared before execution. Source: the clean `billo-guildhall` checkout at `fe79265b2f47937051625719f3ba5080189912c1`. Question: can the two-stage ordinary save keep its existing application timing and store-call arguments while replacing the physical message link crossing the application layer with a plain logical reference?

Compare the actual source `BaseClient.saveMessageToDatabase` and `getTurnConversationFields`, plus the full source `conversations/save.ts`, with an experimental copy that changes only `savedMessageId`/`appendMessageIds` plumbing to logical references. The proposed adapter privately memoizes the physical locator by the returned reference object's identity, validating both logical values and effective owner/tenant scope before reuse. A copied reference uses a scoped lookup; object identity is an optimization, not semantic identity or authority. No application files are edited.

Use the installed TypeScript compiler only to extract/transpile the selected source; load the relevant provider declarations directly from this source checkout. Mock the three legacy store methods and record every call, arguments, return/error and request/client state. Do not load installed application dist or connect to any database. Retain compiler/runtime/source hashes. These are orchestration/translation checks, not real-Mongo conformance, typechecking, authorization-policy proof, concurrency stress or performance measurements.

Planned comparisons: the 12 combinations of unread/null/present request cache, general retention off/on, and initialized false/true; plus disposed client, user mismatch, skip conversation, missing saved result, saved result without physical ID, read/message/conversation failures, conversation error object, no-upsert, endpoint ID override, bound retention, mid-await disposal, mid-await initialized reset, mid-await cache population and endpoint-option mutation. Preserve complete results including differences and unsuccessful attempts.

Compare ordered legacy method calls and exact arguments (including omission versus undefined, Dates, explicit null and empty lists), semantic message/conversation outcome, request cache and initialized state. The intended public message result replaces physical `_id` with a logical `ref`; normalize only those two deliberate fields when comparing message result values. All ordinary same-object reference cases must introduce zero reference-resolution reads. Missing physical ID must preserve the old reconcile path, not fabricate a receipt.

Direct memo cases: same-object hit, equal-valued cloned reference, different binding instance, different tenant, different owner, mutated reference, missing lookup result, and reference wire shape. A lookup miss must not append another row. Detect negative controls: a reference resolver that always looks up (extra-read regression), a resolver that trusts object identity across scope (wrong physical ID), and a detached early snapshot under mid-await initialized reset (different read sequence). Report these as rejected alternatives rather than production defects.

Stop after one planned matrix and control run. If setup or assertions fail, retain that attempt before fixing the harness. No source patch, Mongo test rerun, schema migration, package installation or service restart belongs to this experiment. Success supports a concrete D02 recommendation for the immediate Mongo seam; it does not close Slice02 or settle the DTO census, source-build validation or future RPC retry contract.
