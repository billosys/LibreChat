# P01 result — equal-key message cursor skips rows

One planned attempt, exit 0; two fixtures, both fully inserted into a fresh disposable Mongo database. [Protocol](pagination-protocol.md), [harness](pagination-harness.cjs), [command/attempt](pagination-attempt.json), [raw output](pagination-output.log), [structured result](pagination-result.json).

| Fixture | Stored | Returned | Missing | Duplicates | Termination | Completeness property |
|---|---|---|---|---|---|---|
| Unique whole-second timestamps | 6 | 6 | 0 | 0 | Null cursor | Pass |
| First four share earliest timestamp | 6 | 4 | 2 | 0 | Null cursor | **Fail** |

Both use limit 2, ascending `createdAt`, the same synthetic owner, and separate synthetic conversation IDs. The tied fixture returned `tied-2`, `tied-0`, `tied-4`, `tied-5`, omitting `tied-1` and `tied-3`. Which equal-key rows appear first is unspecified; the finding depends on counts/set membership, not those particular IDs.

The installed method advances by strict greater-than on the timestamp alone, excluding tied rows that did not fit on the first page. The source at `packages/data-schemas/src/methods/message.ts:3583–3619` has the same mechanism. This establishes a concrete completeness failure in the installed build, supporting Slice01's source-based suspicion. It does not establish a source/dist equivalence proof.

Isolation: Mongoose opening was restricted to the exact URI generated by this invocation, `mongodb://127.0.0.1:56845/guildhall_synthetic_cursor`. The default-port target was rejected without connection. Cleanup completed. No live corpus or private export was opened. Runtime Node v22.22.3, Mongoose 8.24.1, MongoMemoryServer 11.0.1 and cached Mongo 8.2.1; hashes and source HEAD are in the result. Source pins Node 24.16.0; no rebuild or upgrade was performed.

Harness success means the control and predicted counterexample matched. It must not be reported as a passing pagination test. No failed setup attempt, retry or skipped planned fixture occurred. Not tested: descending order, subsecond precision, concurrent writes/deletes, authorization, public route behavior or performance. Direct collection insertion deliberately isolates read behavior from save/default/casting behavior.

Disposition: D05's empirical characterization is delivered as contributor evidence. Preserve current behavior in compatibility paths; propose a separately reviewed stable-order/cursor correction. Do not silently bundle a pagination fix into the Mongo adapter. Future corrected conformance must require all six rows exactly once in both fixtures and add the untested edge cases above. Independent reproduction remains pending.
