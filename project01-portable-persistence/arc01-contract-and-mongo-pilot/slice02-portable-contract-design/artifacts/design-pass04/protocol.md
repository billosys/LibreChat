# Pass 4 — per-invocation observed-write ownership

Predeclared before execution; active Slice02, one contributor. No application edits.

Question: can a local turn-write handle hide Mongo linkage while preserving the selected current orchestration, and can it prevent reuse/cross-invocation contamination without adding reference reads? Preserve pass-2 evidence; reuse its 28 fixture definitions and reference execution, extract current source directly with TypeScript, and replace only linkage plumbing in the candidate. Inject mocked stores. A passing comparison requires identical ordered legacy calls/arguments, normalized return/error, request/cache/client state, zero reference-resolution reads, and release of every created handle. Only returned-message `_id` is excluded from comparison; no public ref/token is introduced.

Compile actual tenant context and policy modules against Node AsyncLocalStorage and a controlled strict-mode environment; use the real `resolveTenantScope`. The logger is a double. Do not load application dist, Mongoose middleware, a live DB or private corpus. Record all loaded source hashes, compiler and runtime, protocol/harness/prototype hashes, command, attempts, raw results and failures.

Candidate contract: one synchronous begin, at most one message call, then at most one conversation call, release in caller `finally`. Capture the locator returned by the message method privately. A missing result/locator retains omitted append metadata (legacy reconcile). An observed locator supplies append metadata. No lookup fallback, public physical handle, transaction or automatic retry. Scope is checked before each dispatch and must match the begin scope/owner. Strict missing scope fails. States reject invalid/repeated/overlapping use. Release is idempotent and forbids new stages; a dispatched write may settle but cannot repopulate released state. Keep application decisions at their current post-await points.

Additional deterministic cases: normal lifecycle; conversation-before-message; duplicate/concurrent message; duplicate/concurrent conversation; release before message; release during message; release during conversation; message rejection; conversation rejection; absent result; absent locator; other owner; other tenant; scoped-to-system; system-to-scoped; unscoped-to-scoped; strict missing scope; allowed non-strict unscoped; allowed system scope; strict enabled after begin; two independent overlapping handles completing in reverse order; caller DTO mutation; observed row after simulated deletion/recreation. Record method counts, physical append arguments and terminal state (test-only inspection, not a public production API). No case may depend on timing sleeps.

Negative controls must detect: resolving to replacement B instead of observed A; sharing a locator slot between handles; bypassing scope recheck; omitting caller release; detached early client snapshot. Success requires all comparisons and lifecycle cases plus detection of every mutant. Stop on unexpected failure, preserve attempt, fix only a diagnosed harness defect before rerun. Distinguish candidate rejection from setup/oracle defects.

Limits: mocked storage; no Mongoose authorization, TTL, durability, remote session, general scheduler equivalence, full DTO codec, latency, typecheck or application acceptance. Node 22 remains below the pinned Node 24. Existing Mongo lifecycle results establish the counterexample separately; do not rerun that database experiment without new cause.
