# Pass 5 — read boundary and validation order

Predeclared 2026-09-21, before execution. One-contributor Slice02 investigation. No application edits, live data, dependency installation or database.

1. Extract current public-read middleware and the ordinary reload handler from source. Compare legacy wiring to a named-read port whose Mongo compatibility binding expands the same owner-scoped arguments and projection. Sixteen scenarios cover stored/missing/foreign/child conversations, new sentinel, mismatched IDs, read/ownership errors, active job ownership/tenancy, paused action freshness, by-ID active-job exclusion, empty messages and job lookup error. Require equal calls, HTTP outcome and fetch gating. Add deferred-promise checks that early message success/failure cannot produce a response before admission; no sleeps.
2. Extract the current `saveMessage` function and UUID regex. Exercise missing owner and missing/malformed conversation IDs without allowing any model access. Compare direct legacy behavior with a handle capturing non-enforcing tenant scope, and with pass 4's enforcing begin-time scope. With strict mode and no tenant, the latter must be detected as changing error/skip order. This narrowly answers preflight order, not persistence behavior.
3. Parse provider schemas and CLIENT_MESSAGE_SELECT from source; join them with the sealed pass-4 field census. Account for every declared message/conversation field, default-hidden fields, implicit metadata, provider-only/type-only fields, nested exclusions and getUnsetFields' protected keys. Preserve source hashes and extraction assumptions. This is a static field/projection matrix, not runtime casting or a nested codec proof.

Negative controls: wrong-owner read binding, omitted public projection and enforcing strict scope before legacy preflight must each be detected. Stop on unexpected failures, retain attempt metadata/logs/results, and diagnose rather than changing the oracle to match a faulty candidate. Candidate handle supplies actual currentTenantScope for non-enforcing capture; underlying legacy query methods/plugins still enforce resolveTenantScope. No claim that this fixture exercises Mongoose middleware.

Record exact source head/files, prototype dependency hashes, compiler/runtime, commands and outcomes. Keep earlier packets sealed. Freeze the read/caller boundary only to the extent established by source inspection; explicitly retain nested payload and full consumer/type/build gaps. Node 22 tooling is below the repository's Node 24 pin; this work cannot count as implementation acceptance.
